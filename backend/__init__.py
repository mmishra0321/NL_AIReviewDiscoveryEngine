"""FastAPI backend for the Spotify Discovery AI Review Engine.

Wraps the existing `src/` pipeline + RAG logic in a thin REST API consumed
by the React frontend. Streamlit app is preserved as a fallback UI.
"""
