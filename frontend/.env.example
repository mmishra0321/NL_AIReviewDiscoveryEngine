# Frontend env — copy to .env.local for local overrides
# Default (empty): in dev, vite proxies /api -> http://127.0.0.1:8000.
# Set to the deployed backend URL (e.g. Render) for the production build.
VITE_API_BASE=
